/** * Created by SolmindsDelli5 on 24-05-2016. */$(document).on( "click", "#request", function(){    if(!$(this).next().is(':hidden') ) { //If immediate next container is closed...        $('.acc_trigger').removeClass('active').next().slideUp(); //Remove all .acc_trigger classes and slide up the immediate next container        //$(this).toggleClass('active').next().slideDown(); //Add .acc_trigger class to clicked trigger and slide down the immediate next container    }    if( $(this).next().is(':hidden') ) { //If immediate next container is closed...        $('.acc_trigger').removeClass('active').next().slideUp(); //Remove all .acc_trigger classes and slide up the immediate next container        $(this).toggleClass('active').next().slideDown(); //Add .acc_trigger class to clicked trigger and slide down the immediate next container    }    $('#listrequest').trigger('click');});/*function test() { if(!$('#request').next().is(':hidden') ) { //If immediate next container is closed... $('.acc_trigger').next().slideDown(); //Remove all .acc_trigger classes and slide up the immediate next container //$(this).toggleClass('active').next().slideDown(); //Add .acc_trigger class to clicked trigger and slide down the immediate next container $('#listrequest')[0].click(); } }*/$(function(){    //test();    $('#listrequest').click(function(){        //$('#productaddsection').slideUp('slow');// slide down the project listing div        $('#requestlistsection').slideDown('slow');// slide down the project listing div        $('#listrequest').removeClass('btn-danger').addClass('btn-success');        $('#addrequest').removeClass('btn-success').addClass('btn-danger');        $.ajax({            type: 'POST',            url: '../DoccumentManager/search',            beforeSend : function(){                $('.preloader').show();            },            dataType: "json",            data: {name:$('#searchrequest').val()},            success: function(data){                if(data.error=='No')                {                    $('#requestitems').html(data.result);                    $('#requesttable').show();                }                else                {                    alert(data.errortext);                }                $('.preloader').hide();            }        });    });    $('#requestsearch').click(function(){        $('#listrequest').trigger('click');    });    /*$('#addproduct').click(function(){     $('#productlistsection').slideUp('slow');// slide down the project listing div     $('#productaddsection').slideDown('slow');// slide down the project listing div     $('#addproduct').removeClass('btn-danger').addClass('btn-success');     $('#listproduct').removeClass('btn-success').addClass('btn-danger');     $('.error').hide();     });*/    $('.savereq').click(function(){        var error=0;        $('.error').hide();        /*if($('#Amount').val()=='')         {         $("#Amount").next("span").html('Enter Amount').show('slow');         error=1;         }*/        $('.Amount').each(function(){            var id=$(this).attr('data-id');            if($(this).val()=='')            {                $("#Amount"+id).next("span").html('Enter Amount').show('slow');                error=1;            }        });        $('.details').each(function(){            var id=$(this).attr('data-id');            if($(this).val()=='')            {                $("#Details"+id).next("span").html('Enter Description').show('slow');                error=1;            }        });        $('.billqty').each(function(){            var id=$(this).attr('data-id');            var maxqty=$('#maxqty'+id).val();            if ($(this).val() > maxqty){                $("#Quantity"+id).next("span").html('Quantity cannot be greater than job card quantity').show('slow');                error=1;            }            if (maxqty==0){                $("#resourceid"+id).next("span").html('Max work order created against this resource').show('slow');                error=1;            }        });        if($('#Purpose0').val()=='')        {            $("#Purpose0").next("span").html('Enter Tax').show('slow');            error=1;        }        if($('#project').val()=='0')        {            $("#project").next("span").html('Select Project').show('slow');            error=1;        }        if($('#wbsid').val()=='0')        {            $("#wbsid").next("span").html('Select WBS').show('slow');            error=1;        }        if($('#wono').val()=='')        {            $("#wono").next("span").html('Enter WO No').show('slow');            error=1;        }        if($('#Scope').val()=='')        {            $("#Scope").next("span").html('Enter Body').show('slow');            error=1;        }        if($('#Terms').val()=='')        {            $("#Terms").next("span").html('Enter Terms and conditions').show('slow');            error=1;        }        if($('#Payment').val()=='')        {            $("#Payment").next("span").html('Enter Payment Details').show('slow');            error=1;        }        if($('#function').val()=='none')        {            $("#function").next("span").html('Select Function').show('slow');            error=1;        }        if($('#folder').val()=='none')        {            $("#folder").next("span").html('Select Folder').show('slow');            error=1;        }        if($('#Duration').val()=='0')        {            $("#Duration").next("span").html('Enter Valid Duration').show('slow');            error=1;        }        if($('#Address').val()=='0')        {            $("#Address").next("span").html('Enter Address').show('slow');            error=1;        }        if(NameExists($('#wono').val())=='Yes')        {            $("#wono").next("span").html('WO Number Exists').show('slow');            error=1;        }        if(error==0)        {            return true;        }        else        {            return false;        }    });});$(document).on( "change",".processid", function(){    var id=$(this).val();    var itemid=$(this).attr('data-id');    $.ajax({        type:'POST',        url:'../../DoccumentManager/GetActivity',        dataType:"json",        data:{processid:id,projectid:$('#project').val()},        success:function (data) {            if (data.error == 'No') {                $('#itemid'+itemid).html(data.result);            }        }    });});$(document).on("change", ".itemid", function () {    //var itemid = $(this).attr('data-id');    var itemid = $(this).val();    //var projectid = $('#project').val();    var activityid=$(this).attr('data-id');    var processid = $('#processid'+activityid).val();    $.ajax({        type:'POST',        url:'../../DoccumentManager/Getresources',        dataType:"json",        data:{activityid:itemid,processid:processid},        success:function (data) {            if (data.error == 'No') {                $('#resourceid'+activityid).html(data.result);            }        }    });});$(document).on("change", ".resourceid", function () {    var resid = $(this).val();    var id=$(this).attr('data-id');    var itemid = $('#itemid'+id).val();    $.ajax({        type:'POST',        url:'../../DoccumentManager/GetresDetails',        dataType:"json",        data:{activityid:itemid,resid:resid},        success:function (data) {            if (data.error == 'No') {                $('#Unit'+id).val(data.Unit);                $('#Rate'+id).val(data.Price);                $('.billrate').trigger("blur");                /*var total = data.total;                 $('#totalrate'+requestid).html(data.total);*/                var total = data.Quantity;                $('#maxqty'+id).val(data.maxqty);                $('#Quantity'+id).val(data.Quantity);                $('.billqty').trigger("blur");            }        }    });});$(document).on( "blur",".requestamount", function(){    var itemid=$(this).attr('data-id');    var amount =$(this).val()*1;    var tds=$('#tdsper'+itemid).val();    var tax=$('#taxper'+itemid).val();    var tdsamount=(amount * tds)/100;    var roundtds=Math.round(tdsamount);    var taxamount=(amount * tax)/100;    var totalamount=amount + taxamount;    var netamount=totalamount - roundtds;    $('#accountstds'+itemid).text(roundtds).val();    $('#account_tds'+itemid).val(roundtds).val();    $('#accountservtax'+itemid).text(taxamount).val();    $('#account_tax'+itemid).text(taxamount).val();    $('#accountnet'+itemid).text(netamount.toFixed(2)).val();    $('#account_net'+itemid).val(netamount);    var totalrate=0;    $('.requestamount').each(function(){        totalrate=totalrate+$(this).val()*1;    });    $('#requestratetotal').html(totalrate);    /*var totalnet=0;     $('.account_net').each(function(){     totalnet=totalnet+$(this).val()*1;     });     $('#totalnetamount').html(totalnet);*/});$(document).on( "blur",".account_tds", function(){    var itemid=$(this).attr('data-id');    var tdsamount =$(this).val()*1;    var amount=$('#requestamount'+itemid).val();    var netamount=amount - tdsamount;    $('#accountnet'+itemid).text(netamount.toFixed(2)).val();    $('#netamount'+itemid).text(netamount.toFixed(2)).val();    $('#account_net'+itemid).val(netamount);});$(document).on( "change",".requeststatus", function(){    var status=$('.requeststatus').val();    var totalnet=0;    $('.requeststatus').each(function(){        if($(this).val()==1){            var requestid=$(this).attr('data-id');            if($('#accountshead'+requestid).val()=='0')            {                $("#accountshead"+requestid).next("span").html('Select Account head').show('slow');                $('.error').show();            }            if($('#accounthead'+requestid).val()=='0')            {                $("#accounthead"+requestid).next("span").html('Select Account head').show('slow');                $('.error').show();            }            var netamount=($('#account_net'+requestid).val()*1);            if($('#contraentry'+requestid).is(":checked")) {                totalnet=totalnet;            }            else            {                totalnet=totalnet + netamount;            }        }        else        {            $('.error').hide();        }    });    var cashamount=$('#cashamount').val()*1;    var bankamount=$('#bankamount').val()*1;    var totalamount=cashamount + bankamount;    $('#totalnetamount').html(totalnet.toFixed(2));    $('#totalnetamnt').html(totalnet.toFixed(2));    $('#totalamount').html(totalnet.toFixed(2));    $('#amounttotal').val(totalnet);    /*if(status==1)     {     var requestid=$(this).attr('data-id');     alert(requestid)     var totalnet=0;     }     var requestid=$(this).attr('data-id');     alert(requestid)     */});$(document).on("click",".paymenttype",function(){    var cashamount=0;    var bankamount=0;    $(".paymenttype").each(function(){        var checked=$(this).is(":checked");        var type=$(this).attr('data-type');        if(checked==true){            if(type=='cash')            {                var itemid=$(this).attr('data-id');                cashamount=cashamount + ($('#account_net'+itemid).val()*1);            }            else            {                var itemid=$(this).attr('data-id');                bankamount=bankamount + ($('#account_net'+itemid).val()*1);            }        }    });    //var id=$(this).attr('data-id');    //alert($('#requestamount'+id).val());    //var totalamount=cashamount + bankamount;    $('#cashtotal').html(cashamount.toFixed(2));    $('#cashamount').val(cashamount);    $('#banktotal').html(bankamount.toFixed(2));    $('#bankamount').val(bankamount);    //$('#totalamount').html(totalamount.toFixed(2));    /*     var type=$(this).attr('data-type');     var itemid=$(this).attr('data-id');     var amount=$('#requestamount'+itemid).val();     var cashtotal=0;*/});$(document).on( "change",".accountname", function(){    var requestid=$(this).attr('data-id');    var accountid=$(this).val();    var amount=$('#request_amount'+requestid).val();    var place=$('#place').val();    $.ajax({        type: 'POST',        url: '../../FinanceRequests/accounttds',        dataType:"json",        data:{accountid:accountid,amount:amount,place:place},        success: function(data){            if(data.typeerror=='No'){                $('.paymenttype').prop('checked', false);                $('.paymenttype').attr('disabled', 'disabled');                if(data.error=='No')                {                    $('#accounttds'+requestid).text(data.TDS).val();                    $('#account_tds'+requestid).val(data.TDS);                    $('#ledgerbal'+requestid).text(data.Ledgerbal);                    $('#accountstax'+requestid).text(data.Tax).val();                    $('#serv_tax'+requestid).val(data.Tax);                    $('#accountnet'+requestid).text(data.Netamount).val();                    $('#netamount'+requestid).text(data.Netamount).val();                    $('#account_net'+requestid).val(data.Netamount);                    var netamount=0;                    $('.netamount').each(function(){                        netamount=netamount+$(this).val()*1;                    });                    $('#totalnetamnt').html(netamount);                }                else                {                    alert(data.errortext);                }            }        }    });});$(document).on( "change",".accountshead", function(){    var requestid=$(this).attr('data-id');    var accountid=$(this).val();    var amount=$('#requestamount'+requestid).val();    var place=$('#place').val();    $.ajax({        type: 'POST',        url: '../../FinanceRequests/accounttds',        dataType:"json",        data:{accountid:accountid,amount:amount,place:place},        success: function(data){            if(data.error=='No')            {                $('#accountstds'+requestid).text(data.TDS).val();                $('#account_tds'+requestid).val(data.TDS);                $('#ledgerbal'+requestid).text(data.Ledgerbal);                $('#accountservtax'+requestid).text(data.Tax).val();                $('#account_tax'+requestid).val(data.Tax);                $('#tdsper'+requestid).val(data.Tdsper);                $('#taxper'+requestid).val(data.Taxper);                $('#accountnet'+requestid).text(data.Netamount).val();                $('#account_net'+requestid).val(data.Netamount);                /*var netamount=0;                 $('.account_net').each(function(){                 netamount=netamount+$(this).val()*1;                 });                 $('#totalnetamount').html(netamount.toFixed(2));*/            }            else            {                alert(data.errortext);            }        }    });});//In case if the taxes have effect on total amount/*$(document).on("blur", "#tax", function () {    var totalrate = 0;    $('.itemtotal').each(function () {        totalrate = totalrate + ($(this).val() * 1)    });    var tds=$('#tds').val() * 1;    var vat=$('#vat').val() * 1;    var retention=$('#retention').val() * 1;    var taxrate = $(this).val() * 1;    var taxamount=totalrate * taxrate / 100;    var amountpayable = (totalrate + (totalrate * taxrate / 100)+ (totalrate * tds / 100)+ (totalrate * vat / 100)+ (totalrate * retention / 100));    $('#totalservicetax').val((totalrate * taxrate / 100).toFixed(2));    $('#totaltax').text((totalrate * taxrate / 100).toFixed(2));    $('#totalamt').text(amountpayable.toFixed(2));    $('#totalitem').val(amountpayable.toFixed(2));   /!* $('#taxamount').text(taxamount.toFixed(2));*!/});$(document).on("blur", "#tds", function () {    var totalrate = 0;    $('.itemtotal').each(function () {        totalrate = totalrate + ($(this).val() * 1)    });    var taxrate=$('#tax').val() * 1;    var vat=$('#vat').val() * 1;    var retention=$('#retention').val() * 1;    var tds = $(this).val() * 1;    var taxamount=totalrate * taxrate / 100;    var amountpayable = (totalrate + (totalrate * taxrate / 100)+ (totalrate * tds / 100)+ (totalrate * vat / 100)+ (totalrate * retention / 100));    $('#totaltdspayable').val((totalrate * tds / 100).toFixed(2));    $('#totaltds').text((totalrate * tds / 100).toFixed(2));    $('#totalamt').text(amountpayable.toFixed(2));    $('#totalitem').val(amountpayable.toFixed(2));    /!* $('#taxamount').text(taxamount.toFixed(2));*!/});$(document).on("blur", "#vat", function () {    var totalrate = 0;    $('.itemtotal').each(function () {        totalrate = totalrate + ($(this).val() * 1)    });    var tds=$('#tds').val() * 1;    var taxrate=$('#tax').val() * 1;    var retention=$('#retention').val() * 1;    var vat = $(this).val() * 1;    var taxamount=totalrate * taxrate / 100;    var amountpayable = (totalrate + (totalrate * taxrate / 100)+ (totalrate * tds / 100)+ (totalrate * vat / 100)+ (totalrate * retention / 100));    $('#totalvatpayable').val((totalrate * vat / 100).toFixed(2));    $('#totalvat').text((totalrate * vat / 100).toFixed(2));    $('#totalamt').text(amountpayable.toFixed(2));    $('#totalitem').val(amountpayable.toFixed(2));    /!* $('#taxamount').text(taxamount.toFixed(2));*!/});$(document).on("blur", "#retention", function () {    var totalrate = 0;    $('.itemtotal').each(function () {        totalrate = totalrate + ($(this).val() * 1)    });    var tds=$('#tds').val() * 1;    var vat=$('#vat').val() * 1;    var taxrate=$('#tax').val() * 1;    var  retention= $(this).val() * 1;    var taxamount=totalrate * taxrate / 100;    var amountpayable = (totalrate + (totalrate * taxrate / 100)+ (totalrate * tds / 100)+ (totalrate * vat / 100)+ (totalrate * retention / 100));    $('#totalretetionpayable').val((totalrate * retention / 100).toFixed(2));    $('#totalretention').text((totalrate * retention / 100).toFixed(2));    $('#totalamt').text(amountpayable.toFixed(2));    $('#totalitem').val(amountpayable.toFixed(2));    /!* $('#taxamount').text(taxamount.toFixed(2));*!/});*/$(document).on('submit','#approvalfrom',function(){    var count=0;    $(".requeststatus").each(function(){        var id=$(this).attr('data-id');        if($('#rqststatus'+id).val()==1){            if($('#contraentry'+id).is(":checked")) {                return true;            }            if(!$('.paymentmode'+id+':checked').val())            {                count++;            }        }    });    if(count!=0)    {        alert('Please select payment type');        return false;    }    return true;});$(document).on('click','.denyrequest',function(){    var id=$(this).val();    $.ajax({        type: 'POST',        url: '../FinanceRequests/Deny/'+id,        dataType:"json",        success: function(data){            $('#listrequest').trigger('click');        }    });});$(document).on('click','.deletefundrqstbutton',function(){    var reqid=$(this).val();    var r = confirm("Are you sure you want to delete this Request ?");    if (r == true) {        $.ajax({            type: 'POST',            url: '../DoccumentManager/Delete/',            beforeSend : function(){                $('#deletefundrqstbutton'+reqid).attr("disabled", true);            },            dataType: "json",            data: {reqid:reqid},            success: function(data){                if(data.error=='No')                {                    $('#requestrow'+data.Id).remove();                    $('#listrequest').trigger('click');                }                else                {                    alert(data.errortext);                }                $('#deletefundrqstbutton'+data.Id).attr("disabled", false);            }        });    }    else {        return false;    }});$(document).on('click','.deletefundrqst',function(){    var reqid=$(this).val();    var r = confirm("Are you sure you want to delete this Request ?");    if (r == true) {        $.ajax({            type: 'POST',            url: '../FinanceRequests/DeleteRequest/',            beforeSend : function(){                $('#deletefundrqst'+reqid).attr("disabled", true);            },            dataType: "json",            data: {reqid:reqid},            success: function(data){                if(data.error=='No')                {                    $('#requestrow'+data.Id).remove();                    $('#listrequest').trigger('click');                }                else                {                    alert(data.errortext);                }                $('#deletefundrqst'+data.Id).attr("disabled", false);            }        });    }    else {        return false;    }});/*$(document).on("change", "#project", function () {    //var itemid = $(this).attr('data-id');    var project = $("#project").val();    $.ajax({        type:'POST',        url:'../projects/WbsSearch',        dataType:"json",        data:{project:project},        success:function (data) {            if (data.error == 'No') {                var option = data.result;                $(".wbsid").empty().append(option);                $('#project').prop('disabled', 'disabled');            }            else {                alert(data.errortext);            }        }    });});*/$(document).on("change", ".groupid", function () {    //var itemid = $(this).attr('data-id');    var resourcegroup = $(this).val();    var projectid = $('#project').val();    var requestid=$(this).attr('data-id');    $.ajax({        type:'POST',        url:'../../DoccumentManager/GetItems',        dataType:"json",        data:{resourcegroupid:resourcegroup,projectid:projectid},        success:function (data) {            if (data.error == 'No') {                var option = data.result;                $("#itemid"+requestid).empty().append(option);            }            else {                alert(data.errortext);            }        }    });});/*$(document).on("change", ".itemid", function () {    //var itemid = $(this).attr('data-id');    var itemid = $(this).val();    var projectid = $('#project').val();    var requestid=$(this).attr('data-id');    var resourcegroupid = $('#processid'+requestid).val();    $.ajax({        type:'POST',        url:'../../DoccumentManager/GetItemsDetails',        dataType:"json",        data:{project_estimate_Id:itemid,resourcegroupid:resourcegroupid},        success:function (data) {            if (data.error == 'No') {                var Unit = data.Unit;                $('#Unit'+requestid).val(data.unit);                var Price = data.Price;                $('#Rate'+requestid).val(data.Price);                $('.billrate').trigger("blur");                /!*var total = data.total;                 $('#totalrate'+requestid).html(data.total);*!/                var total = data.Quantity;                $('#Quantity'+requestid).val(data.Maxqn);                $('.billqty').trigger("blur");            }            else {                alert(data.errortext);            }        }    });});*/$(document).on("click", "#approverequest", function () {var error=0;    if($('#aprrovedname').val()=='')    {        $("#aprrovedname").next("span").html('Enter Name').show('slow');        error=1;    }    if($('#Designation').val()=='')    {        $("#Designation").next("span").html('Enter Designation').show('slow');        error=1;    }    if(error==0)    {        return true;    }    else    {        return false;    }});$(document).on("change", ".iowid", function () {    //var itemid = $(this).attr('data-id');    var itemid = $(this).val();    var projectid = $('#project').val();    var requestid=$(this).attr('data-id');    $.ajax({        type:'POST',        url:'../DoccumentManager/GetResourcesdetails',        dataType:"json",        data:{itemid:itemid},        success:function (data) {            if (data.error == 'No') {                var Unit = data.Unit;                $('#Unit'+requestid).val(data.Unit);                var Price = data.Price;                $('#Rate'+requestid).val(data.Price);                $('.billrate').trigger("blur");                /*var total = data.total;                $('#totalrate'+requestid).html(data.total);*/                var total = data.Quantity;                $('#Quantity'+requestid).val(data.Quantity);                $('.billqty').trigger("blur");            }            else {                alert(data.errortext);            }        }    });});$(document).on("change", "#vendor", function () {    //var itemid = $(this).attr('data-id');    var vendor = $(this).val();    var project = $("#project").val();    var requestid=$(this).attr('data-id');    $.ajax({        type:'POST',        url:'../../DoccumentManager/Vendorserach',        dataType:"json",        data:{vendor:vendor},        success:function (data) {            if (data.error == 'No') {                var option = data.result;                $('#Address').html(data.result);            }            else {                alert(data.errortext);            }        }    });});$(document).on("blur", ".billqty", function () {    var itemid = $(this).attr('data-id');    var rate = $('#Rate'+itemid).val()* 1;    var unit = $(this).val()* 1;    total=unit*rate;    $('#totalrate'+itemid).text(total.toFixed(2));    $("#totalamount"+itemid).val(total);    var totalrate = 0;    $('.itemtotal').each(function () {        totalrate = (totalrate)* 1 + ($(this).val() * 1)    });    $('#totalamt').text(totalrate.toFixed(2));    $("#totalitem").val(totalrate);});$(document).on("blur", ".billrate", function () {    var itemid = $(this).attr('data-id');    var unit = $('#Quantity'+itemid).val();    var rate = $(this).val();    total=unit*rate;    $('#totalrate'+itemid).text(total.toFixed(2));    $("#totalamount"+itemid).val(total);    var totalrate = 0;    $('.itemtotal').each(function () {        totalrate = (totalrate)* 1  + ($(this).val() * 1)    });    $('#totalamt').text(totalrate.toFixed(2));    $("#totalitem").val(totalrate);});$(document).on('click','#printvoucher',function(){    $('#cancelrequest').trigger('click');});$(document).on('change','#function',function(){    var department=$('#function').val();    $.ajax({        type: 'POST',        url: '../../doccumentManager/Getfolders',        dataType: "json",        data: {department:department},        success: function(data){            if(data.error=='No')            {                $('#folder').html(data.result);            }        }    });});function NameExists(name){    var retval;    $.ajax({        type: 'POST',        url: '../../DoccumentManager/checkWorkOrdername',        async:false,        data: {name:name,type:$('selectedResTypeId').val()},        success: function(data){            retval=data;        }    });    return retval;}